# Story 3.3 — F5 lab hands-on: chatbot "só sentidos" (Fase A, 7 tools read-only) sobre o estado real da Copa

> **Epic:** [EPIC-003 — "A Final"](../epics/EPIC-003-final-lab.md)
> **Status:** Ready
> **Created:** 2026-06-30 by @sm (River)
> **Sprint context:** Bloco 1 do lab da Final (~2,5-3,5h, EPIC-003 §Dimensionamento). Aluno implanta o McpServer (7 sentidos read-only, Story 2.8 Done) e o chatbot Gemini, explorando o estado real da Copa por conversa. **Empacotamento**, não re-arquitetura — a Fase A já está Done; esta story constrói o produto de workshop entregável (padrão validado Oitavas/Quartas).
> **Depends on:** **Story 3.1** (McpServer precisa estar só-sentidos — "mão" `criar_alerta_ingresso` removida — antes de virar lab; senão o aluno veria uma 8ª tool de ação que não deveria existir na Final) · Story 2.8 (Fase A, 7 tools, Done)
> **Sequenciamento com 3.4:** esta story cobre o **Bloco 1** da aula (F5). O **Bloco 2** (F6, Story 3.4) é sequencial na MESMA sessão e na MESMA branch — ver `[AUTO-DECISION]` sobre guia único, abaixo.
> **Source (Art. IV — rastreabilidade):** ADE-008 Inv 1, 2 · Story 2.8 (AC-2 a AC-5, Fase A Done) · decisão do owner 2026-06-30 (EPIC-003 §Decisões fechadas #3, #4)
>
> **Nota de numeração (@sm AUTO-DECISION):** Terceira story da série 3.x, conforme ordem recomendada pelo `@pm` no épico.

---

## `[AUTO-DECISION]` — Guia consolidado ÚNICO para 3.3 + 3.4 (não dois arquivos separados)

O épico (§"Padrão de artefatos por story de lab") lista `docs/runbooks/final-*-guide.md` genericamente para AMBAS as stories de lab (3.3 e 3.4), e o próprio épico trata F5+F6 como **uma única sessão contínua** (§Dimensionamento: Bloco 0 Abertura → Bloco 1 F5 → Bloco 2 F6 → Bloco 3 Retro — um único dia de aula, não dois). **Decisão:** um **único** guia consolidado (`docs/runbooks/final-portal-guide.md`, molde `quartas-f2-portal-guide.md`), com Fases numeradas cobrindo os 4 blocos. **Esta story (3.3) CRIA o arquivo** com Abertura + Bloco F5; **a Story 3.4 ESTENDE o mesmo arquivo** com Bloco F6 + Retro — mesma lógica de "1 guia, múltiplos blocos" já validada nas Quartas (que cobriu 4 blocos técnicos distintos num único documento). Nome exato confirmado por `@analyst`/`@devops` na execução. Ambas as stories continuam target da MESMA branch curada `lab-a-final` (per tabela de Stories do épico) — não há PR intermediário entre 3.3 e 3.4; o PR fork→main acontece uma vez, ao final de 3.4 (ver Dev Notes de 3.4).

---

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools:
  - "code-review"
  - "security-validation"
  - "pattern-validation"
deployment_owner: "@devops"
content_collaboration: "@analyst"
```

**Rationale:** Story de empacotamento de lab (não de código novo) — a Fase A já é Done (Story 2.8). `@dev` cuida da branch curada e de qualquer ajuste operacional necessário para o deploy funcionar como lab (ex.: variáveis de ambiente, workflow). `@analyst` produz o guia consolidado. `@architect` faz o gate de segurança (regra de ouro: nenhuma tool de ação vazou para o lab) — mesmo padrão de 2.11/2.8. `@devops` confirma o slot da branch `lab-a-final` e orquestra a entrega (fork + PR), governança TFTEC.

---

## Story

**Como** aluno do workshop "Living Lab Azure-Native",
**eu quero** implantar o McpServer (7 sentidos read-only) e configurar o chatbot Gemini apontando para o gateway, explorando o estado real da Copa do Mundo por conversa natural,
**para que** eu compreenda como um chatbot pode consultar dados reais com segurança — a regra de ouro "o chatbot nunca escreve direto no banco" agora vale **por construção**, não por roteamento — usando MCP como protocolo padrão de ferramentas, concluindo a experiência de IA do Living Lab.

---

## Acceptance Criteria

> **Rastreabilidade (Art. IV):** cada AC cita a origem (ADE-008, Story 2.8 Done, ou o padrão de entrega validado nas Quartas).

1. **AC-1 — Branch curada `lab-a-final` criada** no upstream TFTEC, a partir do estado pós-**Story 3.1** (McpServer só-sentidos, sem `criar_alerta_ingresso`) + pós-**Story 3.2** (sem infra n8n órfã). Workflow único, manual, padrão validado Oitavas/Quartas (nome proposto `lab-a-final.yml`, `@devops` confirma).
   *(Fonte: EPIC-003 tabela de Stories, S3; padrão herdado `lab-quartas-de-final.yml`/`lab-oitavas-de-final.yml`.)*

2. **AC-2 — Aluno implanta o McpServer** (Container App, ingress **interno**, atrás do gateway YARP — ADE-004, herdado de 2.5/2.8/2.9 sem mudança) e confirma via `tools/list` que retorna **exatamente 7 tools**, **todas `readOnly: true`** no schema do SDK: `consultar_disponibilidade`, `verificar_ingresso`, `consultar_bracket`, `consultar_partidas`, `consultar_classificacao`, `consultar_time`, `consultar_estadio`.
   *(Fonte: Story 2.8 AC-5 (verificado: `FifaTickerTools.cs`, 7 `[McpServerTool(..., ReadOnly = true)]`); ADE-008 Inv 1 — regra de ouro trivial pós-remoção da "mão".)*

3. **AC-3 — Aluno pluga o frontend** (`Lovable/World Cup Tickets Hub/src/lib/llm/gemini.ts` + `mcpTools.ts`) para consumir o McpServer via gateway; demonstra pelo menos 3 das 7 tools em conversa natural — exemplos herdados de Story 2.8 AC-8: *"Quando o Brasil joga?"* → `consultar_partidas`; *"Como está o grupo A?"* → `consultar_classificacao`; *"Me fala do Maracanã"* → `consultar_estadio`.
   *(Fonte: Story 2.8 AC-8 (exemplos de uso), verificado `gemini.ts`.)*

4. **AC-4 — Regra de ouro demonstrada AO VIVO (o momento didático central do bloco):** o facilitador pede ao aluno que tente uma pergunta de "ação" (ex.: *"cria um alerta pra mim quando abrir ingresso VIP"*) e observa, junto da turma, que o chatbot **não tem mais** essa ferramenta — o LLM não pode inventar uma tool que não existe no `tools/list`. É a prova visual de que não há vetor de escrita, **sem precisar explicar roteamento por Service Bus/webhook** (ADE-008 Rationale — "fortalece a regra de ouro... demonstração mais limpa de IA que consulta mas não corrompe").
   *(Fonte: ADE-008 Inv 1, Inv 2 + Rationale "Por que o chatbot fica só com 'sentidos'".)*

5. **AC-5 — Pin do Gemini documentado (não alterado nesta story):** o guia registra que o `MODEL` runtime default (`gemini.ts` linha 24) já é `gemini-2.5-flash`, mas o **comentário de cabeçalho** do arquivo (linhas 1-9, "Modelo: models/gemini-2.0-flash") está **desatualizado** — inconsistência pré-existente, verificada nesta story, **não corrigida aqui** (o pin é governado pela ADE-002 Inv 3; corrigi-lo é decisão do `@architect`, sinalizada como Risco #2 do épico — "Gemini 2.5-flash shutdown out/2026"). O guia documenta o modelo **realmente em uso** (2.5-flash) para o aluno não se confundir.
   *(Fonte: verificado `gemini.ts` linhas 1-24; EPIC-003 Riscos #2; ADE-002 Inv 3 — fora da autoridade desta story editar.)*

6. **AC-6 — Guia consolidado do aluno** (`docs/runbooks/final-portal-guide.md`, molde `quartas-f2-portal-guide.md`) cobre, para este bloco: pré-requisitos, deploy do McpServer (Portal + workflow), App Settings do gateway para a rota MCP (`McpServerUrl`, herdado de 2.5 — `McpServerDestinationConfigFilter` já existe no `Program.cs` do gateway, verificado), configuração da chave Gemini no proxy server-side (nunca no bundle — `llmProxyBase()`), roteiro de perguntas de exemplo, troubleshooting.
   *(Fonte: padrão Oitavas/Quartas; verificado `Program.cs` do Gateway linhas 55-56 — `AddConfigFilter<McpServerDestinationConfigFilter>()` já existe, reusado sem mudança.)*

7. **AC-7 — Fluxo de entrega 100% web (padrão Quartas), completado ao final de 3.4:** fork novo do aluno com TODAS as branches (Sync fork não traz branch nova) → o aluno acumula os commits de 3.3 (F5) e 3.4 (F6) na branch `lab-a-final` do próprio fork → PR `lab-a-final` → `main` no fork, executado **ao término do Bloco 2** (não nesta story isoladamente — ver `[AUTO-DECISION]` acima).
   *(Fonte: padrão validado `lab-quartas-de-final.yml` — fork novo + PR no próprio fork.)*

8. **AC-8 — Smoke:** `tools/list` via gateway com Bearer CIAM válido retorna 7 tools; requisições `POST /mcp` **não cacheadas** (sem `X-Cache` ou `X-Cache: MISS` — herdado do fix de cache do gateway que corrigiu a interferência entre cache de GET e chamadas MCP/LLM); pergunta ao chatbot sobre disponibilidade de ingresso retorna dado real do SQL (via `FifaQueryRepository`).
   *(Fonte: memória de produto "fix do cache do gateway (quebrava MCP/LLM) commitado"; Story 2.9 Task 6.2 nota "POST não cacheado herdado do fix de cache".)*

9. **AC-9 — Anti-hallucination (Art. IV):** nenhuma tool, endpoint ou claim inventados no material didático — reusa exatamente o contrato MCP e os 7 nomes de tools já `Done` na Story 2.8. O guia não descreve `criar_alerta_ingresso` nem qualquer "mão" (removida em 3.1).

---

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type:** Deployment (empacotamento de lab: branch curada + workflow + guia)
**Secondary Type(s):** Security (validação de que a regra de ouro se sustenta em produção — só leitura), Documentation (guia consolidado)
**Complexity:** MEDIUM (a Fase A já é Done — o trabalho é de empacotamento/entrega, não de nova lógica; a complexidade vem da coordenação entre deploy real + material didático + fluxo de PR)

### Specialized Agent Assignment

**Primary Agents:**
- @dev (Dex) — branch curada, ajustes operacionais de deploy
- @analyst (Atlas) — guia consolidado (Fase F5 do documento único)

**Supporting Agents:**
- @architect (Aria) — quality gate (regra de ouro, security-validation)
- @devops (Gage) — slot da branch, orquestração do workflow, entrega fork+PR

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): `coderabbit --prompt-only -t uncommitted` — foco em nenhuma tool de ação (`ReadOnly=false`) presente
- [ ] Pre-PR (@devops): `coderabbit --prompt-only --base main` — regressão fora do escopo do lab
- [ ] Pre-Deployment (@devops): smoke `tools/list` = 7, `X-Cache` ausente em POST /mcp, chatbot responde com dado real

### Self-Healing Configuration

**Expected Self-Healing:**
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 30 minutes
- Severity Filter: CRITICAL, HIGH

**Predicted Behavior:**
- CRITICAL issues: auto_fix (bloqueia se persistir após 2 iterações)
- HIGH issues: auto_fix (documenta como debt se falhar)

### CodeRabbit Focus Areas

**Primary Focus:**
- Regra de ouro: `tools/list` = exatamente 7, zero `ReadOnly=false` (herda o mesmo focus area da Story 2.8/2.9)
- Chave Gemini nunca no bundle do front (proxy server-side, `llmProxyBase()` — padrão herdado de 2.5)

**Secondary Focus:**
- Guia consolidado não referencia `criar_alerta_ingresso`/n8n em nenhum roteiro de exemplo
- Consistência do pin do modelo Gemini documentado (2.5-flash real vs comentário desatualizado)

---

## Tasks / Subtasks

### Task 1 — Branch + workflow (AC: 1)

- [ ] 1.1 Criar branch curada **`lab-a-final`** no upstream TFTEC, a partir do estado pós-3.1+3.2 (fork/branch de trabalho a confirmar com `@devops`).
- [x] 1.2 Workflow único manual **criado**: `.github/workflows/lab-a-final.yml` (nome confirmado por @devops), padrão `lab-quartas-de-final.yml` (job único `lab`, steps guardados por `if`, sem `needs`). `acao`: **`mcp-server | flow-events | gateway | frontend | tudo`** (ordem do `tudo` = `mcp-server → gateway → flow-events → frontend`, per guia Bloco 3). Blocos derivados sem invenção: `mcp-server` ← `deploy-phase-05.yml`, `gateway` ← `deploy-phase-02.yml` (+ smoke fail-closed de Quartas), `flow-events` ← `deploy-phase-06.yml`, `frontend` = fusão dos jobs `deploy-frontend` de phase-05/06 (chatbot + guard de key + rota `/flow`).

### Task 2 — Deploy McpServer (AC: 2)

- [ ] 2.1 Aluno implanta o Container App do McpServer (ingress interno, atrás do gateway).
- [ ] 2.2 Confirmar `tools/list` = 7 tools, todas `readOnly: true` (regressão da limpeza feita em 3.1).

### Task 3 — Integração Gemini/frontend (AC: 3, 5)

- [ ] 3.1 Configurar App Settings do gateway (`McpServerUrl` ou equivalente já wired por `McpServerDestinationConfigFilter`).
- [ ] 3.2 Configurar a chave Gemini no proxy server-side (`llmProxyBase()` — nunca no bundle).
- [ ] 3.3 Demonstrar ao menos 3 tools em conversa natural (exemplos herdados de 2.8 AC-8).
- [x] 3.4 Documentar no guia o modelo Gemini realmente em uso (2.5-flash) e a inconsistência do comentário de cabeçalho (sem corrigir o código — fora de escopo, ADE-002). *(Guia: Fase 1.5 + Apêndice B — @architect gate 2026-07-02.)*

### Task 4 — Demonstração da regra de ouro (AC: 4)

- [ ] 4.1 Roteiro guiado: facilitador pede uma pergunta de "ação" e a turma observa a ausência da tool — nenhuma escrita é possível.

### Task 5 — Guia consolidado (AC: 6) — @analyst

- [x] 5.1 Criar `docs/runbooks/final-portal-guide.md` (Fase Abertura + Fase F5), molde `quartas-f2-portal-guide.md` (Fases numeradas, App Settings, GitHub Variables/Secrets, deploy via Actions, smoke, apêndices). *(Bloco 0 + Bloco 1 F5, Fases 1-5 + Apêndices A-D — @architect gate 2026-07-02.)*
- [ ] 5.2 Consultar (não copiar) `docs/workshops/phase-05/` (README/PORTAL-GUIDE/SPEAKER-NOTES/slides/intro-video-script — material antigo do EPIC-002, pré-consolidação) como referência histórica do que já foi ensinado sobre MCP/Gemini; não editar esses arquivos (fora de escopo — nota de Art. IV, mesma lógica da Story 3.2).

### Task 6 — Smoke + anti-hallucination (AC: 8, 9)

- [ ] 6.1 Smoke `tools/list` via gateway com Bearer válido.
- [ ] 6.2 Confirmar `X-Cache` ausente/MISS em `POST /mcp`.
- [x] 6.3 Revisão anti-hallucination do guia (nenhuma tool/endpoint inventado). *(Guia sem `criar_alerta_ingresso`/n8n; 7 tools reais; auditoria @qa APROVADA — @architect gate 2026-07-02.)*

---

## Dev Notes

### Estado herdado (o que JÁ está Done e não muda nesta story)

- **7 tools da Fase A** (`src/Fifa2026.V2.McpServer/Tools/FifaTickerTools.cs` — **nota:** o arquivo se chama `FifaTickerTools.cs`, mas declara a classe `FifaTicketTools`; inconsistência de nomenclatura pré-existente, verificada, não corrigida por esta story): `consultar_disponibilidade`, `verificar_ingresso`, `consultar_bracket` (Story 2.5), `consultar_partidas`, `consultar_classificacao`, `consultar_time`, `consultar_estadio` (Story 2.8). Todas `[McpServerTool(..., ReadOnly = true)]`.
- **`FifaQueryRepository.cs`** — só `SELECT` parametrizado via Dapper; cabeçalho "Acesso SOMENTE leitura — o McpServer nunca grava" (verificado).
- **Gateway já roteia para o McpServer:** `src/Fifa2026.V2.Gateway/Program.cs` linha 55-56, `.AddConfigFilter<McpServerDestinationConfigFilter>()` — infraestrutura de roteamento herdada de Story 2.5, **não recriada** por esta story.
- **`gemini.ts`** (`Lovable/World Cup Tickets Hub/src/lib/llm/gemini.ts`) — `MODEL` default já é `gemini-2.5-flash` (linha 24); proxy server-side já protege a API key (`llmFetch`/`proxy.ts`).

### O que esta story FAZ (empacotamento, não código novo)

Diferente da Story 3.1 (que reescreve código), 3.3 é **operacional**: branch curada, deploy guiado, guia consolidado. Qualquer ajuste de código necessário para o deploy funcionar como lab (ex.: variável de ambiente faltando, pequeno fix de config) é esperado ser **mínimo** — a Fase A já foi validada 100% em memória de produto ("MCP Server + proxy Gemini validados 100% no backend").

### Verificação na fonte da verdade (Art. IV)

| Afirmação | Confirmado em |
|---|---|
| 7 tools, todas `ReadOnly = true` | `FifaTickerTools.cs` (grep `McpServerTool`) |
| `FifaQueryRepository` só leitura | `FifaQueryRepository.cs` linhas 1-16 |
| Gateway já tem `McpServerDestinationConfigFilter` | `Gateway/Program.cs` linha 55-56 |
| `gemini.ts` `MODEL` default = `gemini-2.5-flash`, comentário de cabeçalho desatualizado (cita 2.0-flash) | `gemini.ts` linhas 1-24 |
| Material antigo `docs/workshops/phase-05/` existe (5 arquivos) — não editado por esta story | `Glob docs/workshops/phase-05/**` |
| Molde do guia consolidado (Fases numeradas, Apêndices) | `docs/runbooks/quartas-f2-portal-guide.md` (headers verificados) |

### Testing

Story de lab — não há novos testes automatizados de código (a Fase A já tem cobertura via `McpToolCallIntegrationTests.cs`/`McpEndpointTests.cs`, Story 2.8). A validação é:
1. Smoke manual `tools/list` (Task 6.1).
2. Confirmação de cache (Task 6.2) — regressão do fix já commitado.
3. Revisão do guia por `@analyst`/`@po` (Task 6.3).

### Troubleshooting esperado (herdado de 2.5/2.8/2.9 + novo)

| Sintoma | Causa | Mitigação |
|---|---|---|
| `tools/list` retorna 8 (não 7) | Story 3.1 não foi de fato aplicada nesta branch/deploy | Confirmar que a branch `lab-a-final` parte de um estado pós-3.1 (McpServer sem `FifaActionTools.cs`) |
| Chatbot "inventa" uma resposta de ação | LLM alucinando apesar da ausência da tool (function calling não é 100% infalível) | Reforçar didaticamente: mesmo que o texto do LLM prometa uma ação, NENHUMA escrita ocorre — a "promessa" no texto não é uma tool call real; o guia deve deixar isso explícito |
| `POST /mcp` retorna `X-Cache: HIT` (não deveria) | Regressão do fix de cache do gateway | Confirmar que a branch inclui o commit do fix (memória de produto: "fix do cache do gateway... commitado") |

---

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-06-30 | 0.1 | Story drafted — lab F5 (chatbot só-sentidos). 9 ACs derivados de ADE-008 Inv 1/2 + Story 2.8 (Done). `[AUTO-DECISION]` guia consolidado ÚNICO com a Story 3.4 (`final-portal-guide.md`, molde Quartas) em vez de dois arquivos fragmentados — justificado pelo próprio §Dimensionamento do épico tratar F5+F6 como uma sessão contínua. Pin do Gemini (comentário desatualizado em `gemini.ts`) documentado mas não corrigido — fora da autoridade desta story (ADE-002 Inv 3, decisão de `@architect`, Risco #2 do épico). Verificado que a infra de roteamento MCP no gateway já existe (Story 2.5), nada recriado. | @sm (River) |
| 2026-06-30 | 0.1.1 | Validated GO (10/10) — Status: Draft → Ready. 9 ACs rastreiam à ADE-008 Inv 1-2 + Story 2.8; 7 tools ReadOnly=true confirmadas em FifaTickerTools.cs. @po APROVA a AUTO-DECISION do guia único (docs/runbooks/final-portal-guide.md — 3.3 cria Abertura+Bloco F5, 3.4 estende com F6+Retro): consistente c/ precedente Quartas (guia único multi-bloco) + §Dimensionamento do épico (F5+F6 = sessão contínua). Bloco 1 do lab; PR fork→main só ao fim de 3.4. | @po (Pax) |

---

## Dev Agent Record

### Agent Model Used

_Pendente — a preencher pelo `@dev`/`@analyst` na implementação._

### Completion Notes

_Pendente._

### File List

**Criado (@devops):**
- `.github/workflows/lab-a-final.yml` — workflow único do lab (blocos `mcp-server`/`flow-events`/`gateway`/`frontend`/`tudo`). Cobre a AC-1 (parte do workflow) e a Task 1.2. Bloco `mcp-server` aplica os App Settings sensíveis do McpServer como secrets do Container App (`SqlConnectionString`, `GEMINI_API_KEY`, `GROQ`/`MISTRAL` opcionais) e — **novidade vs. `deploy-phase-05.yml`** — o `GATEWAY_SHARED_SECRET` como *secretref* (arma o `X-Gateway-Key`). Bloco `frontend` embute o chatbot com guard anti-vazamento de key.

**Já criado (@analyst, sessão anterior):** `docs/runbooks/final-portal-guide.md`.

**Ainda pendente (não coberto por esta entrega de workflow):**
- Task 1.1 — criação da branch curada `lab-a-final` no upstream TFTEC (a partir do estado pós-3.1/3.2) — ação @devops separada.
- Tasks 2.x/3.x/4.x/6.x — deploy guiado real + smokes (`tools/list`=7, `X-Cache` ausente em `POST /mcp`) executados AO VIVO no Azure (dependem do provisionamento HML — já feito em sessão anterior — e do disparo do workflow).
- Sem mudanças de código de aplicação (empacotamento).

---

## QA Results

### Architect Gate — 2026-07-02 · @architect (Aria) · **PASS (parcial — empacotamento aprovado; pendente validação ao vivo)**

**Escopo do gate:** quality gate arquitetural da entrega de **empacotamento** do Bloco 1 (F5) — o workflow `.github/workflows/lab-a-final.yml` (bloco `mcp-server` + `gateway` + `frontend`) e os ajustes do guia `docs/runbooks/final-portal-guide.md` (Bloco 0 + Bloco 1). **Não** re-executa o code review linha a linha (já concluído: 1º REPROVADO → fixes → APROVADO COM RESSALVAS → 2 ajustes finais, todos verificados no código real).

**Tools:** code-review (delta packaging), security-validation, pattern-validation.

| Dimensão | Veredito | Evidência |
|---|---|---|
| **ADE-008 realizado (F5)** | ✅ PASS | Bloco `mcp-server` faz build/test/deploy do `Fifa2026.V2.McpServer` (7 tools read-only, SDK MCP — ADE-002 Inv 1), ingress **interno preservado** (smoke valida a revisão via `az`, sem curl público — coerente com ADE-004/ADE-008 Inv 2). **Zero n8n** no workflow (sem bloco/env `N8N_*`/`phase-04`). Guia Fase 1.7 afirma `tools/list` = 7 `readOnly:true`. |
| **Regra de ouro (Inv 1)** | ✅ PASS | Nenhuma tool `ReadOnly=false` empacotada; guia Fase 5 demonstra a ausência de "mão" **por construção**; anti-hallucination do guia sem `criar_alerta_ingresso`/n8n (AC-9). |
| **Retro-compat — frontend (bundle único)** | ✅ PASS | O env-block do bloco `frontend` carrega **todas** as VITE_* das Quartas (CIAM + admin + v2) além das novas F5; **fail-fast** aborta se `VITE_CIAM_CLIENT_ID`/`VITE_FUNCTION_V2_URL` vazios (mata o cenário "bundle verde com login/compra mortos"); aceita nome Quartas **ou** prefixado da Final. Fecha o CRITICAL do code review. |
| **Retro-compat — rebuild do gateway** | ✅ PASS | Mesmo Container App/imagem das Quartas; a imagem `lab-a-final` é **superset** (adiciona `mcp-server` ao conjunto confiável do X-Gateway-Key + lê `FlowEventsUrl`) — não remove rotas nem injeção existentes; smoke idêntico ao de Quartas (401 sem token + `/health` 200). Config parcial (McpServerUrl/FlowEventsUrl ausentes) degrada só rotas **novas**, nunca as de Quartas. |
| **Topologia de segurança** | ✅ PASS | `GATEWAY_SHARED_SECRET` aplicado como *secretref* no `ca-mcp` **arma** o X-Gateway-Key (fecha CRITICAL-1/HIGH-2 do débito **quando** rodado com o secret); ausência = **caminho manual preservado** (aviso, não erro, sem sobrescrever). Ingress interno do mcp **não é tocado** (só `update --image`/secret/env) — nenhum perímetro reaberto. Nenhuma key de LLM no bundle (guard). |
| **Padrão de workshop** | ✅ PASS | Job único `lab`, steps guardados por `if`, sem `needs`, `workflow_dispatch` manual, `acao` parametrizável com precedência `input‖var‖default`, aluno roda no fork — **estruturalmente idêntico** a `lab-quartas-de-final.yml`. |
| **Coerência guia↔yml (Fases 1, 8, Bloco 3)** | ✅ PASS | Fase 1.3/1.4/1.6 batem com o bloco `mcp-server`/`gateway` (App Settings sensíveis via secret, McpServerUrl no Portal, smoke interno); Fase 8 Variables/Secrets = 1:1 com os `vars.`/`secrets.` referenciados (inclusive os herdados das Quartas + a nota "fork novo"); ordem do `tudo` (mcp-server → gateway → flow-events → frontend) idêntica entre guia Bloco 3 e a ordem física dos steps. |

**Ressalvas registradas (LOW, não bloqueiam o empacotamento):**
1. O `lab-a-final.yml` **não** redeploya o Functions app — a notificação pós-compra **inline** (ADE-008 Inv 3), cujo trace vive **dentro** do nó Consumer (AC-5 de 3.4), depende de o ambiente vivo estar rodando o código pós-Story 3.1. Os **5 nós renderizam de qualquer forma** (a jornada é *trace/mapper-driven*, o branch n8n saiu do `TraceEventMapper`), mas o momento didático "achar a notificação inline no payload do Consumer" exige o Functions com o código 3.1. **→ Item de validação ao vivo, não defeito de empacotamento.**
2. O fail-fast do frontend guarda só `VITE_CIAM_CLIENT_ID` + `VITE_FUNCTION_V2_URL`; um `VITE_ADMIN_*` ausente degradaria a área `/admin` (workforce) **sem** falhar o build. Aceitável (admin é instrutor-facing e a Fase 8 lista as vars); considerar estender o guard num hardening futuro.
3. A ordem do dropdown `acao` (`flow-events` antes de `gateway`) difere da ordem de execução do `tudo` (`gateway` antes de `flow-events`) — cosmético; o guia é explícito quanto à ordem, sem risco prático.
4. Nota "@devops: confirmar/criar `lab-a-final.yml`" no guia (Fases 1.6 e Bloco 3) ficou **stale** — o workflow já existe. Limpeza de doc, inofensiva.
5. **[Story 4.6 / Diploma + /flow] Decidir/validar a URL semeada do FlowEvents: gateway vs FQDN direto do `ca-flow`.** Fato do código: o gateway aplica `MapReverseProxy().RequireAuthorization()` **blanket** (`Gateway/Program.cs:637-639`), mas o cliente do F6/Diploma (`fetchRecentPurchases`/`fetchDiplomaSummary`) **não envia Bearer** — logo, se `VITE_FLOW_EVENTS_BASE_URL` apontar para `{gateway}/flow-events`, as chamadas voltariam **401**; se apontar para o **FQDN externo do `ca-flow`** (anônimo, fora do `X-Gateway-Key` — ADE-009 Inv 1), funcionam. Ou o `/flow` já está latentemente quebrado via gateway, ou tudo roda pelo FQDN direto do `ca-flow`. **Validar ao vivo** qual URL o pré-provisionamento semeia e confirmar `/flow` + `/diploma` carregando telemetria real. (Não é defeito de empacotamento; é decisão/validação de topologia — o Diploma **não muda o comportamento**, só herda o do F6.)

**Status:** mantido **Ready** (não Done). O empacotamento (workflow + guia F5) está **arquiteturalmente aprovado**; permanecem **pendentes de validação ao vivo** — Task 1.1 (branch curada `lab-a-final` no TFTEC, @devops), Tasks 2.x/3.1-3.3/4.1/6.1-6.2 (deploy guiado + smokes `tools/list`=7, `X-Cache` ausente em `POST /mcp`, chatbot com dado real) e Task 7 (fork + PR). Estes fecham na passada ao vivo (provisionamento HML já feito em sessão anterior).

— Aria (@architect)
